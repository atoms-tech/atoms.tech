/**
 * MCP Types - Main Export
 *
 * Centralized export for all MCP (Model Context Protocol) types.
 *
 * Note: MCP servers handle their own OAuth.
 * No centralized OAuth types needed.
 */

// MCP servers manage their own OAuth credentials
// OAuth configuration is passed directly to MCP servers via environment variables
