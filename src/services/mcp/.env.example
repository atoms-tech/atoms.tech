# ============================================================================
# MCP OAuth Service Environment Variables
# ============================================================================

# Token Encryption
# Used for encrypted-local storage strategy
# Generate with: openssl rand -base64 32
NEXT_PUBLIC_TOKEN_ENCRYPTION_KEY=your-32-character-encryption-key-here

# ============================================================================
# Google OAuth Configuration
# ============================================================================

# Get credentials from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Scopes (space-separated)
GOOGLE_OAUTH_SCOPES=openid profile email https://www.googleapis.com/auth/drive.readonly

# Redirect URI (must match OAuth consent screen)
GOOGLE_REDIRECT_URI=http://localhost:3000/api/mcp/oauth/callback/google

# ============================================================================
# Microsoft OAuth Configuration
# ============================================================================

# Get credentials from: https://portal.azure.com/#view/Microsoft_AAD_RegisteredApps
MICROSOFT_CLIENT_ID=your-microsoft-client-id
MICROSOFT_CLIENT_SECRET=your-microsoft-client-secret
MICROSOFT_TENANT_ID=common

# Scopes (space-separated)
MICROSOFT_OAUTH_SCOPES=openid profile email Files.Read User.Read

# Redirect URI
MICROSOFT_REDIRECT_URI=http://localhost:3000/api/mcp/oauth/callback/microsoft

# ============================================================================
# GitHub OAuth Configuration
# ============================================================================

# Get credentials from: https://github.com/settings/developers
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# Scopes (space-separated)
GITHUB_OAUTH_SCOPES=read:user user:email repo

# Redirect URI
GITHUB_REDIRECT_URI=http://localhost:3000/api/mcp/oauth/callback/github

# ============================================================================
# Custom OAuth Provider Configuration
# ============================================================================

CUSTOM_OAUTH_CLIENT_ID=your-custom-client-id
CUSTOM_OAUTH_CLIENT_SECRET=your-custom-client-secret
CUSTOM_OAUTH_AUTHORIZATION_ENDPOINT=https://oauth.example.com/authorize
CUSTOM_OAUTH_TOKEN_ENDPOINT=https://oauth.example.com/token
CUSTOM_OAUTH_SCOPES=openid profile email
CUSTOM_OAUTH_REDIRECT_URI=http://localhost:3000/api/mcp/oauth/callback/custom

# ============================================================================
# Token Service Configuration
# ============================================================================

# Refresh endpoint (relative to your app)
MCP_TOKEN_REFRESH_ENDPOINT=/api/mcp/oauth/refresh

# Refresh buffer in milliseconds (default: 5 minutes)
MCP_TOKEN_REFRESH_BUFFER_MS=300000

# Storage strategy: memory | session | encrypted-local | cookie
MCP_TOKEN_STORAGE_STRATEGY=session

# Debug mode (enable verbose logging)
MCP_TOKEN_DEBUG=true

# ============================================================================
# Production Settings
# ============================================================================

# In production, update redirect URIs to your domain:
# GOOGLE_REDIRECT_URI=https://yourdomain.com/api/mcp/oauth/callback/google
# MICROSOFT_REDIRECT_URI=https://yourdomain.com/api/mcp/oauth/callback/microsoft
# GITHUB_REDIRECT_URI=https://yourdomain.com/api/mcp/oauth/callback/github

# Use session or cookie storage in production
# MCP_TOKEN_STORAGE_STRATEGY=session

# Disable debug mode in production
# MCP_TOKEN_DEBUG=false
